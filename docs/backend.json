
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Representa um usuário (cliente ou barbeiro).",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "role": { "type": "string", "enum": ["client", "barber"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "email", "role", "createdAt"]
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "description": "Serviços da barbearia.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "durationMinutes": { "type": "number" }
      },
      "required": ["id", "name", "price", "durationMinutes"]
    },
    "Appointment": {
      "title": "Appointment",
      "type": "object",
      "description": "Registro de agendamento.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "clientEmail": { "type": "string", "format": "email" },
        "clientName": { "type": "string" },
        "barberId": { "type": "string" },
        "serviceId": { "type": "string" },
        "serviceName": { "type": "string" },
        "durationMinutes": { "type": "number" },
        "price": { "type": "number" },
        "dataHora": { "type": "object", "description": "Firestore Timestamp" },
        "status": { "type": "string", "enum": ["pendente", "confirmado", "cancelado"] },
        "createdAt": { "type": "object", "description": "Firestore Timestamp" }
      },
      "required": ["clientId", "clientEmail", "barberId", "serviceId", "dataHora", "status"]
    },
    "Notification": {
      "title": "Notification",
      "type": "object",
      "description": "Notificações internas para usuários.",
      "properties": {
        "id": { "type": "string" },
        "toId": { "type": "string", "description": "UID do destinatário" },
        "fromName": { "type": "string", "description": "Nome de quem gerou a ação" },
        "type": { "type": "string", "enum": ["new_appointment"] },
        "message": { "type": "string" },
        "read": { "type": "boolean" },
        "createdAt": { "type": "object", "description": "Firestore Timestamp" }
      },
      "required": ["toId", "fromName", "type", "message", "read", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/User",
      "description": "Perfis de usuários."
    },
    "/services/{serviceId}": {
      "schema": "#/backend/entities/Service",
      "description": "Serviços disponíveis."
    },
    "/appointments/{appointmentId}": {
      "schema": "#/backend/entities/Appointment",
      "description": "Registros de agendamentos."
    },
    "/notifications/{notificationId}": {
      "schema": "#/backend/entities/Notification",
      "description": "Notificações para barbeiros e clientes."
    }
  }
}
