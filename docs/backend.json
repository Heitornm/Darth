
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user (client or barber).",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["client", "barber"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "email", "role", "createdAt"]
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "description": "Barbershop service.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "durationMinutes": { "type": "number" }
      },
      "required": ["id", "name", "price", "durationMinutes"]
    },
    "Appointment": {
      "title": "Appointment",
      "type": "object",
      "description": "Scheduled booking.",
      "properties": {
        "id": { "type": "string" },
        "clientId": { "type": "string" },
        "clientName": { "type": "string" },
        "barberId": { "type": "string" },
        "serviceId": { "type": "string" },
        "serviceName": { "type": "string" },
        "startTime": { "type": "string", "format": "date-time" },
        "endTime": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pendente", "confirmado", "cancelado"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "clientId", "barberId", "serviceId", "startTime", "endTime", "status"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/entities/User",
      "description": "User profiles."
    },
    "/roles_barber/{barberId}": {
      "schema": "#/entities/User",
      "description": "Barber role check."
    },
    "/services/{serviceId}": {
      "schema": "#/entities/Service",
      "description": "Available services."
    },
    "/appointments/{appointmentId}": {
      "schema": "#/entities/Appointment",
      "description": "Booking records."
    }
  }
}
